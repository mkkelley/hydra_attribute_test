<%= form_for(@attribute) do |f| %>
  <% if @attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attribute.errors.count, "error") %> prohibited this attribute from being saved:</h2>

      <ul>
      <% @attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :entity_type %><br />
    <%= f.text_field :entity_type, value: 'Employee' %>
  </div>
  <div class="field">
    <%= f.label :backend_type %><br />
    <%= f.text_field :backend_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
